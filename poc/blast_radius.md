# Blast radius

Regenerated by `python run.py graph`. Reflects metadata + templates vs. `manifest/hash_manifest.json` at the time it was rendered.

**Legend:**
- 🔴 red = metadata changed, template changed, or object/template is new (direct hit)
- 🟡 yellow = unchanged itself, pulled in because something it `depends_on` changed (propagated impact) - only applies to data objects, not templates
- 🟢 green = untouched, hash matches the manifest, not regenerated
- rounded nodes connected by dashed lines are templates, not data objects - an object using a changed template is *itself* red/yellow via the diff engine's own template-hash tracking, independent of this diagram

```mermaid
flowchart LR
    bronze_orders_raw["bronze.orders_raw\n(source)"]
    bronze_order_items["bronze.order_items\n(source)"]
    silver_orders["silver.orders\n(generation target)"]
    silver_order_item_totals["silver.order_item_totals\n(generation target)"]
    gold_fact_orders["gold.fact_orders\n(generation target)"]
    bronze_orders_raw --> silver_orders
    bronze_order_items --> silver_order_item_totals
    silver_orders --> gold_fact_orders
    silver_order_item_totals --> gold_fact_orders
    template_cast_and_dedupe(["template: cast_and_dedupe"])
    template_cast_and_dedupe -.-> silver_orders
    template_aggregate(["template: aggregate"])
    template_aggregate -.-> silver_order_item_totals
    template_cast_dedupe_join(["template: cast_dedupe_join"])
    template_cast_dedupe_join -.-> gold_fact_orders
    class gold_fact_orders,template_cast_dedupe_join changed
    class bronze_order_items,bronze_orders_raw,silver_order_item_totals,silver_orders,template_aggregate,template_cast_and_dedupe unaffected
    classDef changed fill:#f8b4b4,stroke:#c0392b,stroke-width:2px,color:#000
    classDef affected fill:#fde9a0,stroke:#d4a017,stroke-width:2px,color:#000
    classDef unaffected fill:#d5f5e3,stroke:#27ae60,stroke-width:1px,color:#000
```

**Pending:** 1 changed/new, 0 pulled in downstream, 4 untouched.
**Templates changed:** cast_dedupe_join.
Run `python run.py diff` then `python run.py generate` to clear this.
